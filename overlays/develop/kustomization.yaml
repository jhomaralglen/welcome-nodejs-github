bases:
  - ../../base

resources:
  - sealed-secrets/docker-pull.yaml
  - sealed-secrets/github-credential.yaml
  - sealed-secrets/webhook-interceptor.yaml
  - sealed-secrets/environment-variables-welcome-nodejs-github-develop.yaml

commonLabels:
  app: welcome-nodejs-github-dev
  env: dev

patchesStrategicMerge:
  - deploymentconfig.patch.yaml 
  - service.patch.yaml
  - ingress.patch.yaml 

transformers:
  - suffix-transformer.yaml
