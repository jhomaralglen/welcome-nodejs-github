apiVersion:  networking.k8s.io/v1
kind: Ingress
metadata:
  name: welcome-nodejs-github
spec:
  tls: 
  - {}
  rules:
  - host: welcome-nodejs-github.apps.dev.thebrewery.app
    http:
      paths:
        - pathType: ImplementationSpecific
          path: "/"
          backend:
            service:
              name: welcome-nodejs-github
              port:
                name: http